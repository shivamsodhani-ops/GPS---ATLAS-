# GPS ATLAS &mdash; frontend

React + Vite + Tailwind SPA. See the [project README](../README.md) for setup, architecture, and how this talks to the backend.

Quick dev loop:
```bash
npm install
npm run dev   # http://localhost:5173, proxies /api to http://127.0.0.1:8000
```
